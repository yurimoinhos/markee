// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/aggi-tech/aggipay/ent/automationrun"
)

// AutomationRun is the model entity for the AutomationRun schema.
type AutomationRun struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// OwnerUserID holds the value of the "owner_user_id" field.
	OwnerUserID string `json:"owner_user_id,omitempty"`
	// AutomationType holds the value of the "automation_type" field.
	AutomationType string `json:"automation_type,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// Details holds the value of the "details" field.
	Details *string `json:"details,omitempty"`
	// RanAt holds the value of the "ran_at" field.
	RanAt time.Time `json:"ran_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AutomationRun) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case automationrun.FieldID, automationrun.FieldOwnerUserID, automationrun.FieldAutomationType, automationrun.FieldStatus, automationrun.FieldDetails:
			values[i] = new(sql.NullString)
		case automationrun.FieldRanAt, automationrun.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AutomationRun fields.
func (_m *AutomationRun) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case automationrun.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = string(value.String)
			}
		case automationrun.FieldOwnerUserID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field owner_user_id", values[i])
			} else if value.Valid {
				_m.OwnerUserID = value.String
			}
		case automationrun.FieldAutomationType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field automation_type", values[i])
			} else if value.Valid {
				_m.AutomationType = value.String
			}
		case automationrun.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case automationrun.FieldDetails:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field details", values[i])
			} else if value.Valid {
				_m.Details = new(string)
				*_m.Details = value.String
			}
		case automationrun.FieldRanAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field ran_at", values[i])
			} else if value.Valid {
				_m.RanAt = value.Time
			}
		case automationrun.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AutomationRun.
// This includes values selected through modifiers, order, etc.
func (_m *AutomationRun) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AutomationRun.
// Note that you need to call AutomationRun.Unwrap() before calling this method if this AutomationRun
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AutomationRun) Update() *AutomationRunUpdateOne {
	return NewAutomationRunClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AutomationRun entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AutomationRun) Unwrap() *AutomationRun {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AutomationRun is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AutomationRun) String() string {
	var builder strings.Builder
	builder.WriteString("AutomationRun(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("owner_user_id=")
	builder.WriteString(_m.OwnerUserID)
	builder.WriteString(", ")
	builder.WriteString("automation_type=")
	builder.WriteString(_m.AutomationType)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	if v := _m.Details; v != nil {
		builder.WriteString("details=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("ran_at=")
	builder.WriteString(_m.RanAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AutomationRuns is a parsable slice of AutomationRun.
type AutomationRuns []*AutomationRun
