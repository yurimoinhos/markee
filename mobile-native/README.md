# Mobile Native (Expo)

Aplicativo nativo inicial (React Native + Expo) compartilhando o contrato OpenAPI do backend.

## Comandos

```bash
npm install
npm run generate:types
npm run start
```

## Variáveis

- `EXPO_PUBLIC_BACKEND_API_URL` (default: `http://127.0.0.1:8000/api/v1`)
